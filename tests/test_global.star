z = 6
zf = 1.2

def x1(x: int) -> int:
  return 2 + x

def xf(x: float) -> float:
  return 2.2 + x

def main() -> int:
  # test reference of global variable
  # test calling function with zero arguments
  b.print(1.5)
  zz = 6.4
  zz = zf + 1.5 + zz
  zz = zz + 1.0
  z = z + 1
  return z - 9 + x1(0)
